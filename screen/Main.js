import React from 'react'
import { Button, StyleSheet, Text, View } from 'react-native';

function Main({navigation}) {
    return (
        <View>
            <ScrollView>
                
            </ScrollView>
        </View>
    )
}

export default Main
